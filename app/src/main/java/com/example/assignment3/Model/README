How to initialize the board:
For testing purposes only

public class Main {
    private static final int ROWS = 5;
    private static final int COLS = 4;
    private static final int MINES = 20;
    public static boolean[][] mine = new boolean[ROWS][COLS];

    public static void main(String []arg){
        Board board = new Board(ROWS,COLS,MINES);
        mine = board.initializeBoardWithMines();
        for(int row = 0; row < ROWS; row++){
            for(int col = 0; col < COLS ; col++){
                System.out.println("MINE AT ROW = " + row + " COLUMN " + col + " is " + mine[col][row]);
            }
        }
    }
}
